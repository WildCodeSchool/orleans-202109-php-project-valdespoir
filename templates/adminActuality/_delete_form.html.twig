<main class="container-fluid">
<form method="post" action="{{ path('admin_actualite_supprimer', {'id': actuality.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actuality.id) }}">
    <button class="btn btn-outline-dark rounded fs-5">Supprimer</button>
</form>
</main>
