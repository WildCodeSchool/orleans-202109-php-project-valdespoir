<form method="post" action="{{ path('admin_actuality_delete', {'id': actuality.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actuality.id) }}">
    <button class="btn btn-outline-dark rounded fs-5">Supprimer</button>
</form>
