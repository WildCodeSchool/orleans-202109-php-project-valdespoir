{% extends 'base.html.twig' %}

{% block title %}Éditer un chantier
{% endblock %}

{% block body %}
	<main class="container-fluid">
		<h1 class="text-center text-dark mt-5 mb-5">Éditer un chantier</h1>
		{{ include('adminSite/_form.html.twig', {'button_label': 'Éditer'}) }}
        {{ include('adminSite/_delete_form.html.twig') }}
		<div class="mt-5">
			<a class="text-decoration-none" href="{{ path('admin_site_index') }}">< Retour</a>
        </div>
    </main>
{% endblock %}
