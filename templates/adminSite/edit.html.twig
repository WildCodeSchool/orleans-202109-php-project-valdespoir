{% extends 'base.html.twig' %}

{% block title %}Edit Site
{% endblock %}

{% block body %}
<main class="container-fluid">
	<h1 class="d-flex justify-content-center mt-5 mb-5 text-dark">Éditer un chantier</h1>
	{{ include('adminSite/_form.html.twig', {'button_label': 'Éditer'}) }}
	{{ include('adminSite/_delete_form.html.twig') }}
	<div class="mt-5">
		<a class="text-decoration-none" href="{{ path('admin_chantier_index') }}">< retour</a>
    </div>
</main>
{% endblock %}
