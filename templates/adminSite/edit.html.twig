{% extends 'base.html.twig' %}

{% block title %}Edit Site
{% endblock %}

{% block body %}
	<h1 class="d-flex justify-content-center mt-5 mb-5">Modifier un chantier</h1>
	{{ include('adminSite/_form.html.twig', {'button_label': 'Éditer'}) }}
	{{ include('adminSite/_delete_form.html.twig') }}
	<div class="mt-5">
		<a class="text-decoration-none ms-5" href="{{ path('admin_chantier_index') }}">< retour</a>
    </div>
{% endblock %}
