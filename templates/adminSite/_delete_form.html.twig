<form method="post" action="{{ path('admin_site_delete', {'id': site.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ site.id) }}">
    <button class="btn btn-outline-dark rounded fs-5 me-2 mt-2 mb-2">Supprimer</button>
</form>
