<main class="container container-form">
	{{ form_start(form) }}
	<div class="row">
		{{ form_widget(form) }}
	</div>
	<div>
		<button class="btn btn-dark opacity-85 rounded">{{ button_label|default('Ajouter') }}</button>
	</div>
	{{ form_end(form) }}
</main>
