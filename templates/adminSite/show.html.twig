{% extends 'base.html.twig' %}

{% block title %}Chantier{% endblock %}

{% block body %}
<main class="container-fluid">
    <h1 class="text-center text-dark mt-5 mb-5">Chantier</h1>

    <table class="table-responsive">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ site.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ site.description }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ site.city }}</td>
            </tr>
            <tr>
                <th>Image 1</th>
                <td>{{ site.beforePicture }}</td>
            </tr>
            <tr>
                <th>Image 2</th>
                <td>{{ site.afterPicture }}</td>
            </tr>
        </tbody>
    </table>
    <a href="{{ path('admin_chantier_editer', {'id': site.id}) }}">edit</a>

    {{ include('adminSite/_delete_form.html.twig') }}
    <div class="mt-5">
    <a href="{{ path('admin_chantier_index') }}">< Retour</a>
    </div>
</main>
{% endblock %}
