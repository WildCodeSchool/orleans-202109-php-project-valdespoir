<main class="container mt-5 mb-4">
	<div class="row g-0">
		<div class="col-md-12 col-lg-12 col-xl-5 mt-4 mb-3">
			<div id="carouselSiteControls" class="carousel slide" data-bs-ride="carousel">
				<div class="carousel-inner">
					{% for site in sites %}
						<div class="carousel-item {{ loop.first ? 'active' : '' }}">
							<img class="img-fluid d-block w-100 " src="{% if site.afterPicture is not empty %}{{ vich_uploader_asset(site, 'afterPictureFile') }}{% else %}{{ asset('build/images/placeholder-site.jpg') }}{% endif %}" alt="site-image">
							<div class="card-header text-light bg-dark opacity-75 text-center">{{ site.title }}</div>
						</div>
						{% else %}
						<div class="carousel-item {{ loop.first ? 'active' : '' }}">
							<img class="img-fluid d-block w-100 " src="{{ asset('build/images/placeholder-site.jpg') }}" alt="site-image">
							<div class="card-header text-light bg-dark opacity-75 text-center">Pas de réalisation récente</div>
						</div>
					{% endfor %}
				</div>
				<button class="carousel-control-prev" type="button" data-bs-target="#carouselSiteControls" data-bs-slide="prev">
					<span class="carousel-control-prev-icon" aria-hidden="true"></span>
					<span class="visually-hidden">Previous</span>
				</button>
				<button class="carousel-control-next" type="button" data-bs-target="#carouselSiteControls" data-bs-slide="next">
					<span class="carousel-control-next-icon" aria-hidden="true"></span>
					<span class="visually-hidden">Next</span>
				</button>
			</div>
		</div>
		<div class="offset-md-0 col-md-12 offset-lg-0 col-lg-12 offset-xl-1 col-xl-6 mt-4 mb-3">
			<div class="card-body bg-light opacity-75">
				<h2 class="card-title text-dark text-center mb-3">Nos dernières réalisations</h2>
				<p class="card-text text-dark mb-4">Nous proposons différents services d'entretien et d'aménagement des espaces verts:
			<br>
			Tonte des pelouses, taille des haies, désherbage manuel, installation de mobilier extérieur, création de marches en bois et garde-corps, bûcheronnage et débroussaillage, arrachage de plantes invasives dans les rivières, fabrication et pose de clôtures.</p>
				<a href="{{ path('site_index') }}" type="button" class="btn site-btn btn-primary text-light">En savoir plus</a>
			</div>
		</div>
	</div>
</main>
