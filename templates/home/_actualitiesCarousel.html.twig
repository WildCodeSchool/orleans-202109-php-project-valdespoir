<main class="container mt-5 mb-4">
	<div class="row g-0">
		<div class="col-md-12 col-lg-12 col-xl-6  mt-4 mb-4">
			<div class="card-body bg-light opacity-75">
				<h2 class="card-title text-dark text-center mb-3">Nos dernières Actualités</h2>
				<p class="card-text text-dark mb-4">Gardez le contact avec Val espoir ! <br> Venez découvrir la vie au sein de notre association qui tient à vous informer régulièrement de toutes ses dernières actualités. </p>
				<a button type="button" class="btn btn-primary text-light" href="{{ path('actuality_index') }}">En savoir plus</a>
			</div>
		</div>
		<div class="offset-md-0col-md-12 col-lg-12 offset-lg-0 col-xl-5 offset-xl-1 mt-4 mb-4">
			<div id="carouselActualityControls" class="carousel slide " data-bs-ride="carousel">
				<div class="carousel-inner">
					{% for actuality in actualities %}
					<div class="carousel-item {{ loop.first ? 'active' : '' }}">
						<img class="img-fluid d-block w-100 " src="{% if actuality.picture is not empty %}{{ vich_uploader_asset(actuality, 'pictureFile') }}{% else %}{{ asset('build/images/placeholder-site.jpg') }}{% endif %}" alt="actuality-image">
						<div class="card-header text-light bg-dark opacity-75 text-center">{{ actuality.title }}</div>
					</div>
					{% else %}
					<div class="carousel-item {{ loop.first ? 'active' : '' }}">
						<img class="img-fluid d-block w-100 " src="{{ asset('build/images/placeholder-site.jpg') }}" alt="actuality-image">
						<div class="card-header text-light bg-dark opacity-75 text-center">Pas d'actualité récente</div>
					</div>
					{% endfor %}
				</div>
				<button class="carousel-control-prev" type="button" data-bs-target="#carouselActualityControls"
					data-bs-slide="prev">
					<span class="carousel-control-prev-icon" aria-hidden="true"></span>
					<span class="visually-hidden">Previous</span>
				</button>
				<button class="carousel-control-next" type="button" data-bs-target="#carouselActualityControls"
					data-bs-slide="next">
					<span class="carousel-control-next-icon" aria-hidden="true"></span>
					<span class="visually-hidden">Next</span>
				</button>
			</div>
		</div>
	</div>
</main>