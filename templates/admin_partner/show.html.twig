{% extends 'base.html.twig' %}

{% block title %}Partenaire{% endblock %}

{% block body %}
    <h1>Partenaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partenaire.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partenaire.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ partenaire.picture }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ partenaire.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_partner_index') }}">back to list</a>

    <a href="{{ path('admin_partner_edit', {'id': partenaire.id}) }}">edit</a>

    {{ include('admin_partner/_delete_form.html.twig') }}
{% endblock %}
