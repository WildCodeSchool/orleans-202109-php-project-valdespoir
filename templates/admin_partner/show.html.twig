{% extends 'base.html.twig' %}

{% block title %}Partner{% endblock %}

{% block body %}
    <h1>Partner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ partner.picture }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ partner.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('index') }}">back to list</a>

    <a href="{{ path('editer', {'id': partner.id}) }}">edit</a>

    {{ include('admin_partner/_delete_form.html.twig') }}
{% endblock %}
