{% extends 'base.html.twig' %}

{% block title %}Edit Partner
{% endblock %}

{% block body %}
	<main class="container-fluid">
		<h1 class="text-dark text-center mb-5 mt-5">Éditer un partenaire</h1>
		{{ include('admin_partner/_form.html.twig', {'button_label': 'Editer'}) }}
		<div class="ms-2">
			{{ include('admin_partner/_delete_form.html.twig') }}
		</div>
		<div class="mt-5">
			<a class="text-decoration-none ms-2" href="{{ path('partner_index') }}">Retour</a>
		</div>
	</main>
{% endblock %}
